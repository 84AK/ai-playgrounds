# 작업 로그 (Work Log) - 2026년 3월 28일 (최종 통합본)

## 1. 작업 개요
- **목표**: 쇼케이스 데이터 가독성 향상, MBTI 데이터 로딩 오류 해결, 피드백 팝업 개선 및 배포 환경(Vercel) 안정성 확보
- **담당**: 건축가(Blueprint), 작업자(Worker), 해결사(Fixer), 디자이너(UI Polish), 서기(Doc) 협업

## 2. 수정 및 구현 내용

### [쇼케이스 학교 이름 자동 통합 (Normalization)]
- **파일**: `app/showcase/page.tsx`
- **내용**: 
    - 중구난방인 학교 이름(`인천 대건고`, `대건 고등학교`, `인천대건`)을 `인천대건고등학교`로 자동 통합.
    - 드롭다운 필터의 중복 제거 및 검색 편의성 향상.

### [MBTI 데이터 로딩 호환성 수정]
- **파일**: `app/mbti/play/page.tsx`, `app/mbti/result/page.tsx`
- **내용**:
    - 구글 스프레드시트 헤더 대소문자(`Text` vs `text`) 불일치로 인한 UI 빈 칸 문제 해결.
    - 모든 데이터 매핑 로직에 대소문자 방어 코드(`data.Text || data.text`) 적용.

### [선생님 피드백 팝업 개선 및 영구 닫기]
- **파일**: `components/FeedbackOverlay.tsx`
- **내용**:
    - **레이아웃**: `max-h-[85vh]`와 스크롤을 적용하여 모바일/데스크탑 무관하게 긴 메시지 잘림 방지.
    - **마크다운**: `react-markdown`으로 강조 서식 적용.
    - **영구 닫기**: `localStorage`를 연동하여 확인을 누르면 동일 메시지는 다시 나타나지 않음.

### [배포 환경 런타임 에러(Client-side Exception) 해결]
- **파일**: `app/showcase/page.tsx`
- **내용**:
    - **Fetch 가드**: APPS_SCRIPT_URL이 비어있거나 API가 HTML(에러 페이지)을 반환할 때 크래시되는 현상 방지.
    - **불변성**: `reverse()` 호출 시 복사본을 사용하여 리액트 상태 관리 안정성 확보.

## 3. 발생했던 에러 및 해결 과정
- **문제 1**: 카드 클릭 시 질문 내용 미표시 (대소문자 불일치 -> 방어 코드로 해결)
- **문제 2**: 배포 후 쇼케이스 페이지 "Application Error" (환경 변수 누락 -> Fetch 예외 처리 및 가이드 제공으로 해결)

## 4. 향후 참고 사항
- **Vercel 설정**: 배포 대시보드에서 `NEXT_PUBLIC_APPS_SCRIPT_URL` 환경 변수를 반드시 등록해야 쇼케이스가 정상 작동함.
- **필터링**: 이름에 숫자가 있거나 `남은`으로 시작하는 테스트 계정은 자동으로 필터링됨.

## 5. 아크랩스 홈페이지
- [아크랩스(AKLABS)](https://litt.ly/aklabs)
