# 작업 로그 (Work Log) - 2026-04-13

## 📋 작업 개요
노션(Notion) 페이지 연동 기능 추가 및 다수 관리자 환경을 위한 보안/안정성 가이드 구현

## 🛠 구현 및 수정 내용
1.  **노션 API 엔진 구축 (`lib/notion.ts`)**
    - 별도의 라이브러리 설치 없이 `fetch`를 이용한 노션 REST API 통신 구현
    - 노션 데이터베이스 쿼리 및 페이지 블록 페칭 로직 작성
    - 노션 블록(Heading, Paragraph, Image 등) -> Markdown 변환기 구현

2.  **데이터 소스 하이브리드화 (`lib/courseContent.ts`)**
    - 구글 시트와 노션을 선택적으로 사용할 수 있는 로직 통합
    - `Notion Priority` 모드 도입: 노션에 데이터가 있을 경우 우선 노출, 없을 경우 시트나 로컬 폴백

3.  **보안 강화 및 암호화 (`lib/crypto.ts`, `app/api/admin/encrypt`)**
    - **RULE 1** 준수를 위해 노션 API 키 암호화 저장 방식 채택
    - 서버사이드 AES-256-CBC 암호화 유틸리티 구현
    - 클라이언트에서 민감 정보를 안전하게 서버로 전달하여 암호화하는 프록시 API 추가

4.  **관리자 UI 업데이트**
    - `app/admin/setup/page.tsx`: 노션 연동 섹션 추가, 데이터 소스 우선순위 토글 버튼 구현
    - `app/admin/page.tsx`: **"안정적인 수업 운영 가이드"** 섹션 추가 (Vercel/GAS 성능 및 제한 사항 안내)

## 🐛 해결된 문제 및 처리 사항
- **보안 이슈:** API 키가 브라우저에 평문으로 남는 문제 -> 서버사이드 암호화 저장으로 해결
- **성능 이슈:** 다수 관리자 사용 시 동시 실행 제한 우려 -> 대시보드 내 개별 백엔드 사용 가이드 추가로 해결

## 📚 향후 참고 사항
- 노션 데이터베이스의 속성명은 `Track`(텍스트), `Week`(텍스트/숫자), `Title`(제목)을 기본으로 하며, 대소문자를 구분하지 않도록 유연하게 처리함.
- `ENCRYPTION_MASTER_KEY`를 Vercel 환경 변수에 설정하여 보안 강도를 조절할 수 있음.
