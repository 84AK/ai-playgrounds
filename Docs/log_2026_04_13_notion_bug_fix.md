# 작업 로그 (Work Log) - 2026-04-13 (2차)

## 📋 작업 요약
노션 연동 통합 중 발생한 빌드 에러 해결 및 커리큘럼-컨텐츠 하이브리드 동기화 완료

## 🛠 수정 및 보완 내용
1. **빌드 에러 해결 (`lib/courseContent.ts`)**
   - **에러 내용:** `Identifier 'getNotionCourseContent' has already been declared`
   - **원인:** 파일 상단(33번 라인)과 중간(108번 라인)에 동일한 함수가 중복으로 `import`되어 식별자 충돌 발생.
   - **조치:** 108번 라인의 중복 `import` 문을 제거하고 구문 오류(Syntax Error)를 수정하여 빌드 안정성 확보.

2. **노션 전용 최적화 필터링 (로컬 필터링)**
   - API 쿼리의 엄격한 타입 제한을 피하기 위해, 전체 데이터를 가져온 후 앱 내부에서 `toUpperCase()`, `trim()` 등을 적용해 대소문자나 공백에 구애받지 않고 트랙/주차 정보를 정확히 매칭하도록 로직 고도화.

3. **하이브리드 엔진 최종 통합**
   - 대시보드 커리큘럼 목록(Structure)과 개별 강의 내용(Content) 모두에 대해 노션 설정을 우선 참조하도록 연동.
   - 노션 데이터 로드 실패 시 구글 시트로 자동 전환되는 폴백(Fallback) 메커니즘 검증.

## 🐛 해결된 주요 이슈
- **Module parse failed:** 중복 선언으로 인한 서비스 중단 문제 해결.
- **데이터 불일치:** 노션 데이터베이스의 속성 대소문자 차이로 인해 데이터가 표시되지 않던 문제 해결.

## 📚 향후 참고 사항
- 작업 중 생성된 `lib/notion.ts`와 `lib/courseContent.ts`는 노션과 구글 시트를 동시에 지원하는 핵심 로직입니다.
- 추가적인 데이터베이스 속성 변경 시 `lib/notion.ts`의 `getNotionCourseStructure` 내 매핑 로직을 확인하면 됩니다.
- 아크랩스 홈페이지 링크: https://litt.ly/aklabs
